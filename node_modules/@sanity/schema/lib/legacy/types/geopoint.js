'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.GeoPointType = undefined;

var _pick2 = require('lodash/pick');

var _pick3 = _interopRequireDefault(_pick2);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

const OVERRIDABLE_FIELDS = ['jsonType', 'type', 'name', 'title', 'description', 'options'];

const GEOPOINT_CORE = {
  name: 'geopoint',
  type: null,
  jsonType: 'object'
};

const GeoPointType = exports.GeoPointType = {
  get() {
    return GEOPOINT_CORE;
  },
  extend(subTypeDef) {
    const parsed = Object.assign((0, _pick3.default)(GEOPOINT_CORE, OVERRIDABLE_FIELDS), subTypeDef, {
      type: GEOPOINT_CORE
    });

    return subtype(parsed);

    function subtype(parent) {
      return {
        get() {
          return parent;
        },
        extend: extensionDef => {
          const current = Object.assign({}, parent, (0, _pick3.default)(extensionDef, OVERRIDABLE_FIELDS), {
            type: parent
          });
          return subtype(current);
        }
      };
    }
  }
};