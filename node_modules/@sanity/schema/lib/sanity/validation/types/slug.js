'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var _createValidationResult = require('../createValidationResult');

exports.default = (typeDef, visitorContext) => {
  const problems = [];

  if (typeDef.options && typeDef.options.slugifyFn) {
    problems.push((0, _createValidationResult.warning)('Heads up! The "slugifyFn" option has been renamed to "slugify".', _createValidationResult.HELP_IDS.SLUG_SLUGIFY_FN_RENAMED));

    typeDef.options.slugify = typeDef.options.slugifyFn;
  }

  return _extends({}, typeDef, {
    _problems: problems
  });
};