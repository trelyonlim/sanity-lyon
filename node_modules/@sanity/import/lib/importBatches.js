'use strict';

let importBatches = (() => {
  var _ref = _asyncToGenerator(function* (batches, options) {
    const progress = progressStepper(options.onProgress, {
      step: 'Importing documents',
      total: batches.length
    });

    const mapOptions = { concurrency: DOCUMENT_IMPORT_CONCURRENCY };
    const batchSizes = yield pMap(batches, importBatch.bind(null, options, progress), mapOptions);

    return batchSizes.reduce(function (prev, add) {
      return prev + add;
    }, 0);
  });

  return function importBatches(_x, _x2) {
    return _ref.apply(this, arguments);
  };
})();

function _asyncToGenerator(fn) { return function () { var gen = fn.apply(this, arguments); return new Promise(function (resolve, reject) { function step(key, arg) { try { var info = gen[key](arg); var value = info.value; } catch (error) { reject(error); return; } if (info.done) { resolve(value); } else { return Promise.resolve(value).then(function (value) { step("next", value); }, function (err) { step("throw", err); }); } } return step("next"); }); }; }

const pMap = require('p-map');
const progressStepper = require('./util/progressStepper');

const DOCUMENT_IMPORT_CONCURRENCY = 3;

function importBatch(options, progress, batch) {
  const client = options.client,
        operation = options.operation;

  return batch.reduce((trx, doc) => trx[operation](doc), client.transaction()).commit({ visibility: 'async' }).then(progress).then(res => res.results.length);
}

module.exports = importBatches;